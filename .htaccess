<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirect seluruh request ke folder /public secara otomatis
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
